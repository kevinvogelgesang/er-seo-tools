# Database — SQLite
# For local dev, use a relative path
# For RunCloud, use an absolute path outside the app directory so it persists across deploys
# Example: DATABASE_URL="file:/var/lib/er-seo-tools/prod.db"
DATABASE_URL="file:./prisma/dev.db"

# Port (Next.js defaults to 3000)
PORT=3000
