# ER SEO Tools — Claude Context

Internal SEO toolkit for Enrollment Resources. Next.js 15 App Router, TypeScript, Tailwind CSS (class-based dark mode), Prisma + SQLite. Deployed on RunCloud (not Vercel/serverless).

## Stack constraints
- **Node 22** on production
- **SQLite only** — no Postgres/MySQL
- **No serverless** — RunCloud + PM2
- **Google Chrome** must be installed on the server (`/usr/bin/google-chrome`) for ADA audits
- **Do not change the core stack** unless explicitly asked

## Deploy
Always `git push` before SSHing — the server pulls from GitHub.

```bash
ssh seo@144.126.213.242 "~/deploy.sh"
```

- **App path:** `/home/seo/webapps/seo-tools`
- **DB:** `/home/seo/data/seo-tools/db.sqlite`
- **Uploads:** `/home/seo/data/seo-tools/uploads`
- **Logs:** `/home/seo/logs/`
- **In-build type-check/lint are DISABLED** (`next.config.ts` `typescript.ignoreBuildErrors` + `eslint.ignoreDuringBuilds`, 2026-07-11 deploy-OOM fix — the extra build workers OOM'd the 3.9 GB box twice while the app was resident). Local gates (`tsc --noEmit` + vitest) are the ONLY type-check gate: never merge without them, and never re-enable the in-build checks "for safety" without solving server build memory first.

## Improvement-roadmap handoff protocol

Triggered when a session completes (or meaningfully advances) an item in
`docs/superpowers/todos/2026-06-10-improvement-roadmap-tracker.md`, **or**
whenever Kevin asks for "the handoff prompt" / "handoff". Do all three, in order:

1. Update the tracker: checkbox status + a dated status-log line.
2. Rewrite `docs/superpowers/todos/HANDOFF-improvement-roadmap.md` — current
   state, the single next item with key context, gotchas. Commit it together
   with the tracker change.
3. **End your final reply with the handoff doc's "Paste this into a new chat"
   prompt in a code block**, so Kevin can copy it straight into the next chat.

## Schema changes
1. Edit `prisma/schema.prisma`
2. `npx prisma migrate dev --name <name>` — creates migration + regenerates client locally
3. Production migration runs automatically via `prisma migrate deploy` in the deploy command

## Key files
- `components/ThemeProvider.tsx` — React context for dark/light mode; reads `localStorage('er-theme')` + `prefers-color-scheme`; toggles `.dark` class on `<html>`
- `components/ThemeToggle.tsx` — Sun/Moon toggle button rendered in Nav; waits for `mounted` to avoid hydration mismatch
- `lib/db.ts` — Prisma client singleton (import as `import { prisma } from '@/lib/db'`)
- `lib/api/` — A3 route kit: `with-route.ts` `withRoute()` wraps a handler in a uniform error envelope (`HttpError`→status+code; Prisma `P2025`→404/`P2002`→409 as a last-resort net; unknown→500 `internal_error` with no message leak; a thrown or returned `Response` passes through untouched — auth stays in middleware, NOT in `withRoute`) + `body.ts` `parseJsonBody()` (malformed JSON → 400 `invalid_json`); `errors.ts` defines `HttpError`. New API routes should wrap their handler in `withRoute` and parse JSON bodies with `parseJsonBody`.
- `lib/log/` — A4 structured logger (`pino`): `logger` + `logError(context, err)`. Prod = plain JSON to **stderr** (PM2 error log); `pino-pretty` is dev-only; construction is guarded (an invalid `LOG_LEVEL` can NOT crash-loop boot) and `logError` NEVER throws into callers (protects the worker `onExhausted` seam). Adopted at seams only (job-worker error/exhausted/tick/settle — keeps the `[jobs]` subsystem tag as a `subsystem` field), NOT a wholesale `console.*` migration. Server-only; never `.toString()`-injected.
- `lib/ops/` (A4 observability, atop D0's health layer) — `health-summary.ts` (`computeHealthAlerts(now)` = the SINGLE degraded-flag source shared by `/api/health` + `/admin/ops`; `getLivenessSummary` = TTL-cached/lookback-`since`/fail-open wrapper for the public poll), `ops-snapshot.ts` (`loadOpsSnapshot()` = per-section fault-isolated loader — a failed panel degrades to `{ok:false}` + is logged, never blanks the page), `disk.ts` (`getDiskFree` via `fs.statfs`), `db-size.ts` (`getDbSizeBytes` = main+`-wal`+`-shm`, hardened `file:` URL parse). (D0's `health-check.ts`/`backup.ts`/`alert-*` unchanged.)
- `lib/ada-audit/types.ts` — shared types for axe-core results (`StoredAxeResults`, `AxeViolation`, `AuditDetail`, etc.)
- `lib/ada-audit/browser-pool.ts` — singleton headless Chrome + page pool (size 2); `acquirePage()` / `releasePage()` / `closeBrowser()`
- `lib/ada-audit/runner.ts` — axe-core runner via puppeteer-core; SSRF protection, progress callbacks, `wcagTags` expansion
- `lib/ada-audit/scoring.ts` — `computeScore(violations, wcagLevel)` → `{ score: 0–100, compliant: boolean }`
- `lib/ada-audit/sitemap-crawler.ts` — `discoverPages(domain)` with robots.txt, wp-sitemap, gzip support, 1000-page hard cap
- `lib/ada-audit/queue-manager.ts` — global FIFO queue: `enqueueAudit()` / `processNext()` / `resetStaleAudits()` / `recoverQueue()`
- `lib/ada-audit/queue-order.ts` — **C14 sales-overhaul PR3** the ONE home of the site-audit queue's total ordering (prospect-owned first → `createdAt` → `id`): pure `compareQueuedAudits`, promoter pick `findNextQueuedAudit()` (two `findFirst`s — Prisma can't express "non-null first" without value-ordering prospectId), position `queuedAheadCount()`, and `PROSPECT_DISCOVER_PRIORITY=1`. Imports ONLY `@/lib/db` (so `listProspects` + the site-audit detail route can import it without dragging queue-manager's finalizer→findings graph). ALL FOUR queue readers adopt it (`processNext`, `getQueueStatus`, `listProspects`, `GET /api/site-audit/[id]`); never re-derive the ordering inline. `processNext` stamps the discover job `priority=1` for prospect audits so an already-enqueued UNCLAIMED non-prospect discover job is out-claimed (worker `claimNext` orders `[priority desc, createdAt asc]`) — NO preemption. The prospect DELETE route demotes any still-queued discover job back to priority 0 (a `SetNull`'d-orphan audit must not keep prospect priority)
- `lib/jobs/system-schedules.ts` — code-owned `system-*` Schedule rows (cleanup daily@09:00, screenshot-sweep every:30m, stale-audit-reset every:10m, db-backup daily@08:00 — fresh snapshot before cleanup's retention deletes, health-alert every:15m), seeded idempotently at boot; `system-` is a reserved namespace
- `lib/jobs/handlers/scheduled-site-audit.ts` — C2 wrapper job fired by client-owned Schedule rows: resolves its Schedule via the Job row, re-validates client+domain (config rot disables the schedule, DB errors retry), then enters `queueSiteAuditRequest()`
- `lib/ada-audit/carry-forward-checks.ts` — `carryForwardSiteAuditChecks()` copies `SiteAuditCheck` rows by content key from the previous completed same-domain audit (fire-and-forget in the finalizer, invoked before the findings hook)
- `lib/ada-audit/scheduled-retention.ts` — `pruneScheduledSiteAudits()`: cadence-aware deletion of schedule-originated terminal SiteAudits (weekly 90 d / monthly 365 d / daily 14 d priced-in; keep latest 2 completed per schedule); CrawlRun findings survive via SetNull; in `runCleanup()`
- `lib/services/client-schedules.ts` — `getClientSchedules()`: per-client scan schedules + last-run join; scores from `CrawlRun.score` by `siteAuditId` (the finalizer never persists `SiteAudit.score`)
- `lib/services/site-audit-diff.ts` — C3 instance diffing: `getSiteAuditInstanceDiff()` (domain+wcagLevel-matched previous, anchored at the audit's own run) + `getRunPairInstanceDiff()` (null on level mismatch or non-ada runs); pure classifier `diffInstances()` lives in `lib/services/findings-shared.ts` (keyed on `Finding.dedupKey`, page-set-aware: regressed vs new-page, resolved vs not-rescanned)
- `lib/ada-audit/findings-fallback.ts` — C3 read-time fallbacks for pruned blobs: `buildSummaryFromFindings()` (degraded `SiteAuditSummary` from `CrawlPage`/`Violation` rows + unpruned child scalars) + `buildArchivedAxeResults()` (degraded `StoredAxeResults`, `archived: true`, capped nodes, no screenshots); `archivedCounts` carries nullable pass/incomplete — UI renders "—", never a literal 0
- `lib/findings/` — normalized findings layer (A2): `types.ts` (**the source-agnostic ingestion contract (C5)** — adapters produce a `FindingsBundle`; `source` includes `'live-scan'` (C6 broken-link verifier); **C6 landed `@@unique([siteAuditId, tool])`** — a SiteAudit now carries up to two runs (ada-audit + seo-parser live-scan); `findUnique`/`update` on a SiteAudit-origin run use `{ siteAuditId_tool: { siteAuditId, tool } }`, `deleteMany`/`count`/`findMany` use plain `{ siteAuditId, tool }`), `seo-mapper.ts`/`ada-mapper.ts` (blob → `CrawlRun`/`CrawlPage`/`Finding`/`Violation` bundles), `writer.ts` (delete-and-recreate in one array-form txn, `createMany` chunked at 50), `seo-write.ts`/`ada-write.ts` (best-effort dual-write hooks — a findings failure must never fail the legacy path), `parity.ts` (blob-vs-tables comparator), `normalize-url.ts` (client-safe URL normalizer), `retention.ts` (`pruneArchivedBlobs()`, 90-d blob archive — `PRUNE_ACTIVATED`: ada-audit ACTIVE since C3 (origin + child blobs + screenshot artifacts), seo-parser ACTIVE since C5 (`Session.result`)), `keys.ts` (sha256 dedup keys)
- `lib/findings/seo-findings-fallback.ts` — C5 read-time fallback for pruned `Session.result` blobs: `buildSeoResultFromRun()` (pure) + `loadArchivedSeoResult(sessionId)` build a degraded `AggregatedResult` (`archived: true`, safe-shape arrays, run-scope Findings → issues with page-scope URL lists, status buckets only when `CrawlPage.statusCode` exists — never inferred); completeness intentionally absent — UI suppresses the recompute and shows `ArchivedSessionBanner`
- `scripts/findings-rebuild.ts` / `scripts/findings-parity.ts` — rebuild findings rows from an origin blob / verify blob-vs-tables parity (`npx tsx`, id type auto-detected)
- `lib/report/` — C4 reporting layer (all relational-first; works on archived audits): `csv.ts` (RFC-4180 + formula-injection neutralization + `safeFilenamePart` for Content-Disposition), `wcag-criteria.ts` (static WCAG A/AA table + axe-tag→criterion mapping), `vpat.ts` (two-state VPAT 2.4 markdown scaffold — Does Not Support / Not Evaluated, never "Supports"), `escape.ts`/`report-html.ts` (pure branded-report HTML builders, every dynamic string escaped), `report-data.ts` (`loadSiteReportData()` — findings-run-only, child-blob screenshots ≤6 with traversal guard, level-matched trend), `report-file.ts` (`REPORTS_DIR` PDF store, atomic write, ENOENT-tolerant delete)
- `lib/jobs/handlers/report-render.ts` — durable on-demand PDF render (concurrency 1, 2 attempts, group/dedup `report:<siteAuditId>` — NEVER `site-audit:<id>`, that group means audit liveness to recovery): data+HTML built BEFORE `acquirePage()`, only `setContent`+`page.pdf` while holding the page, deleted-audit no-ops, log-only `onExhausted`
- `lib/notify/` — D7 scan-completion email (Mailgun): `config.ts` (single env home — `isNotifyEnabled()` dark gate = `MAILGUN_API_KEY && MAILGUN_DOMAIN`; `mailgunConfig`, `notifyFrom`/`notifyReplyTo`/`notifyAdminEmail`, `notifyRequestTimeoutMs`), `transport.ts` (`sendEmail` — plain-`fetch` Mailgun Messages API, Basic `api:<key>`, form-encoded, injectable `NotifyDeps`, AbortController timeout, NEVER logs the key), `content.ts` (pure `buildCompleteEmail`/`buildFailedEmail`, HTML-escaped, tolerates missing SEO run)
- `lib/jobs/handlers/notify-email.ts` — D7 durable notifier (concurrency 1, 3 attempts, dedupKey `notify-email:<id>:<kind>`, **NO `site-audit:<id>` group** — `failSiteAudit` cancels that group). Idempotency = durable `SiteAudit.notify{Complete,Failed}SentAt` markers (dedupKey is active-window only); flow = read-marker → send → conditional stamp (at-least-once, narrow dup window). No-op on: dark env, deleted row, null recipient, marker set, `NEXT_PUBLIC_APP_URL` unset. `enqueueNotifyEmail(id, kind)`
- `lib/ada-audit/link-harvest.ts` — C6 pure rendered-DOM `<a href>`/`<img src>` harvest (`harvestLinks`/`classifyTargets`); same-domain = exact-host + www-insensitive (subdomains external in v1), cap 300/page. **C6 Phase 2:** `harvestLinks` ALSO runs `parseSeoFromDocument` in the SAME `page.evaluate` (string-injected) → returns `pageSeo` (zero extra round-trips)
- `lib/ada-audit/seo/parse-seo-dom.ts` — C6 Phase 2 pure **self-contained** rendered-DOM on-page SEO parser (`parseSeoFromDocument(doc, win)` → `RawPageSeo`: title/meta/robots-noindex/canonical/h1/h2/visible-word-count/JSON-LD `@type`+`@graph`/hreflang/image alt+dim/weighted login-like). Injected via `.toString()` so it MUST reference NO module scope and emit NO escaping SWC helper (`typeof`→`_type_of` would `ReferenceError` in-page — avoid `typeof`; verified clean at es2017)
- `lib/ada-audit/broken-link-check.ts` — C6 single-URL check (`checkUrl`: HEAD→GET to avoid HEAD false positives; SafeUrlError/network/timeout → `unconfirmed`, excluded from broken) + `HostThrottle` (per-host min delay, no wait on first request); transport-injectable via `realDeps`
- `lib/findings/broken-link-mapper.ts` — C6 `mapBrokenLinkFindings(broken, { runId, ensurePage, … })` → `FindingInput[]` (run-scope count = distinct broken targets; **page-scope keyed by SOURCE PAGE** `pageFindingKey(type, sourcePageUrl)`, NOT target — avoids `@@unique([runId,dedupKey])` collision; `affectedSource:'live-scan-verify'`). C6 Phase 2 refactor: returns findings against an INJECTED runId+`ensurePage` (the builder owns the run/page map), not a whole bundle
- `lib/findings/onpage-seo-mapper.ts` — C6 Phase 2 pure `mapOnPageSeoFindings(rows, { runId, ensurePage, harvestTruncated })` → on-page `FindingInput[]` (`affectedSource:'live-scan-onpage'`). Reuses `deriveIssueTypesForPage` (`issue-membership.ts`) for `missing_title`/`missing_h1`/`missing_meta_description`/`thin_content`; duplicate_title/meta/h1 by trimmed-EXACT value, **run-scope count = number of duplicate GROUPS** (SF `pageTitles.parser` semantics); aggregation set = indexable (statusCode 2xx ∧ html ∧ ¬noindex) ∧ ¬login-like
- `lib/findings/live-seo-score.ts` — C6 Phase 3 pure `scoreLiveSeo(inputs): number | null` (forked `computeHealthScore` with EXPLICIT factor availability — indexability/error/missing-title-meta-h1/thin/schema; crawl-depth + broken-links NEVER in the denominator). Null when `attempted===0` ∨ `observed/attempted < 0.5` ∨ `indexableScored===0` (noindex/login-walled → unscoreable; a partially-noindex site still scores). Called in the builder (all inputs in hand before transient deletion); result → live-scan `CrawlRun.score`. `observed` = HarvestedPageSeo row count, NOT `pagesComplete`
- `lib/jobs/handlers/broken-link-verify.ts` — C6 durable `broken-link-verify` job = the **single live-scan run builder** (concurrency 1, bounded internal `BROKEN_LINK_CONCURRENCY` workers sharing one `HostThrottle`, cap `BROKEN_LINK_MAX_CHECKS`=2000, 15-min timeout, group `site-audit:<id>` — safe ONLY because enqueued post-terminal). It owns ONE `runId` + a shared `ensurePage(url, scalars?)` map, reads BOTH `HarvestedPageSeo` (→ CrawlPage scalars + on-page findings) and `HarvestedLink` (→ verify → broken findings), merges into ONE live-scan `CrawlRun`, writes once, deletes BOTH transient tables. Idempotent; empty-harvest still writes a clean run; `enqueueBrokenLinkVerify()` fired LAST in `finalizeSiteAudit`
- `lib/ada-audit/seo/content-similarity.ts` — C6 Phase 5 pure lexical near/exact-duplicate detection: `computeContentSimilarity(pages, opts)` → sha256 exact-dup + MinHash-128 candidate→exact-Jaccard-refine near-dup (0.9) over 5-word shingles after a document-frequency boilerplate filter (`boilerplateDfMin=3` absolute floor stops small-site over-drop); union-find groups, MIN-pairwise `similarity`, `exactSubgroups` on mixed groups; deterministic fixed-seed `Math.imul` hashing (NO `Math.random`/`Date.now`); returns null for <2 eligible; input is bounded transient `HarvestedPageSeo.contentText` (raw text NEVER durable). NOT a Finding, NO score change — result → nullable `CrawlRun.contentSimilarityJson`; read-time `ContentSimilaritySection`
- `lib/ada-audit/seo/content-signals.ts` — C12 Increment B pure `computeContentSignals(pages, {currentYear})` → nullable `CrawlRun.contentSignalsJson` `{v,observedPages,truncatedPages,staleDates,readability}`: stale-date detection (copyright marker/range LATEST-year ≤`currentYear-2` · season+year<currentYear `term` · enrollment-keyword+year<currentYear `deadline`; bare years never flag; per-page hit cap 5 in DOCUMENT order across rule kinds; per-list page cap 50) + Flesch FRE/FK readability (word `/[A-Za-z]+(?:'[A-Za-z]+)*/g`, sentence `[.!?]+`≥1, vowel-group syllables w/ silent-`e`; formulas + `Math.round(x*10)/10` rounding PINNED to fixtures — retuning a constant breaks tests). Fully pure (`currentYear` injected — NO `Date`/`Math.random`/IO); returns null when no page has `contentText`. Ordinary Node module (NOT `.toString()`-injected — no SWC/`typeof` contract). Computed in the live-scan builder over the SAME indexable∧¬login-like set BEFORE the similarity block (reserve `CONTENT_SIGNALS_RESERVE_MS 10s + CONTENT_SIM_RESERVE_MS 30s`), fail-to-null, `{v:1,...}`-wrapped on `bundle.run`; input is transient `HarvestedPageSeo.contentText` (never durable). NOT a Finding, NO score change; read-time `ContentSignalsSection` (results-page SEO tab, share view unchanged). Migration `20260712000000`. Spec/plan: `docs/superpowers/archive/specs/2026-07-11-c12-tier0-content-signals-design.md` + `archive/plans/2026-07-11-c12-tier0-content-signals.md`
- `lib/ada-audit/seo/topic-overlap.ts` — C12 Tier-1 pure `clusterByTopicOverlap(pages, opts?)` → nullable `CrawlRun.topicOverlapJson` `{v,observedPages,clusteredCandidates,threshold,weights,bodyPrefixTruncatedPages,inputCapped,clustersCapped,clusters[]}`: SEMANTIC keyword-cannibalization (complements the LEXICAL `contentSimilarityJson` + query-based Increment-A cannibalization). Each page = a topic signature (title+H1+meta) vector + a body-intro vector; combined pair sim = `W_SIG 0.6·cos(sig)+W_BODY 0.4·cos(body)`; **BOTH vectors required** to be a clustering candidate (homogeneous metric under one `THRESHOLD 0.78`); single-linkage union-find → connected "topic-overlap networks" (transitive — NOT an all-pairs claim), `minEdgeSimilarity` = weakest DIRECT edge; caps `MAX_CLUSTERS/MAX_MEMBERS 50` with explicit honest flags (`size` stays true under member truncation); returns null iff `clusteredCandidates<2` (≥2 with no edge → non-null `clusters:[]` "analyzed, clean"). Ordinary Node module (NOT `.toString()`-injected — reuses `embedTexts`/`cosineSimilarity` from `lib/services/pillarAnalysis/embeddings.ts`, the LOCAL MiniLM-L6-v2 ONNX singleton, zero API); deterministic (no Math.random/Date.now), pinned constants + bridge fixture. NOT a Finding, NO score change; read-time `components/site-audit/TopicOverlapSection.tsx` (results-page SEO tab, share view unchanged). Migration `20260712120000`. Spec/plan: `docs/superpowers/archive/specs/2026-07-11-c12-tier1-topic-overlap-design.md` + `archive/plans/2026-07-11-c12-tier1-topic-overlap.md`
- `lib/ada-audit/seo/embed-chunked.ts` — C12 Tier-1 cooperative `embedChunked(texts, {embed, yieldFn?, shouldAbort?, chunkSize?})` → `number[][] | null`: splits a large local-ONNX embed call into bounded chunks (default 32) with an inter-chunk event-loop yield (default `setImmediate` macrotask) + a per-chunk `shouldAbort` deadline check → abandons to `null` (no partial) if the deadline passes. `@xenova/transformers` runs ONNX SYNCHRONOUSLY on the JS thread, so this keeps the embedding pass off the job-worker heartbeat's critical path (the pdfjs event-loop-starvation incident is the cautionary tale). Order-preserving, dependency-injected (the builder passes the real `embedTexts` + a `deps.now()`-based deadline). Computed in the live-scan builder (`broken-link-verify.ts`) BETWEEN the content-signals and content-similarity blocks; reserve `TOPIC_OVERLAP_RESERVE_MS 45s` (`shouldAbort` protects the downstream `CONTENT_SIM_RESERVE_MS`); ALL eligible pages passed to the clusterer (null vecs for non-candidates) so `observedPages`=eligible while only both-text candidates embed; fail-to-null
- `lib/content-audit-token.ts` + `lib/content-audit/` — C12 D1 `cat_` content-audit handoff bridge (Increment-D bridge, Option C — the zero-billing skill-handoff stand-in for Tier-2 AI data-correctness; proves the finding schema a future Anthropic job would reuse). `content-audit-token.ts` = stateless `cat_` JWT (audience `content-audit-client` is the isolation wall, shares `KEYWORD_MEMO_TOKEN_SECRET` — **no new prod env var**, sub=siteAuditId, 1h). `lib/content-audit/`: `ingest-schema.ts` (pure `validateContentAuditFindings` — typed `{data_inconsistency|stale_claim|quality_issue}` + severity + evidence`[{url,snippet}]` + recommendation; rejects unknown enums, per-field caps, **aggregate 256KB byte cap**, and **every evidence url must be in the audit's eligible page set** — never truncates), `route-auth.ts` (`requireContentAuditToken` — the ONE fail-closed helper for all 3 public routes; every failure → controlled 401, never a raw throw → 500), `manifest.ts` (`loadContentAuditManifest`/`loadContentAuditPageText`/`contentAuditEligibleUrls` — the single `isIndexable`≡builder-aggregation-set filter, **read-time `retainUntil<=now` expiry** independent of the sweep, 410=in-set-expired vs 404=not-in-set), `read-bounded-json.ts` (streamed byte cap → 413 regardless of Content-Length). **Retention reversal (Kevin sign-off — reverses C6-P5 "contentText transient by design"):** the builder stamps `SiteAudit.contentAuditRetainUntil = now+CONTENT_AUDIT_BASE_TTL_MS` (2h) **BEFORE** `writeFindingsRun` (crash-safe) and NO LONGER deletes `HarvestedPageSeo` (HarvestedLink delete kept); mint extends monotonically to token life; `sweepExpiredContentAudit` (in `lib/findings/retention.ts`, runCleanup + stale-audit-reset) DELETEs rows past expiry, **integer-ms bind + EXISTS-guarded on the seo-parser run** (a crash-window stranded audit — stamp-but-no-run — is never swept early; waits for the 7-d `pruneHarvestedPageSeo` backstop); `recoverBrokenLinkVerifies` scan bounded to `crawlRuns:{none:{tool:'seo-parser'}}`. Ingested findings land on nullable `CrawlRun.contentAuditJson` (migration `20260713000000`) via PATCH, **last-writer-wins, measurement-first — NOT a Finding, NO score change**. Routes: cookie-gated `POST/GET /api/site-audit/[id]/content-audit[/mint-token]` (mint guards complete+live-scan-run+not-archived, honest `textAvailable`; poll `{minted,contentAuditJson}`) + **public** `GET /api/content-audit/[id]/manifest`·`/page`·`PATCH /findings` (3 anchored single-segment middleware matchers — NEVER a `/api/content-audit/` prefix). UI `components/site-audit/ContentAuditCard.tsx` on the results SEO tab (mint→`cat_` clipboard via `lib/content-audit-prompt.ts` [`Webapp:` = `NEXT_PUBLIC_APP_URL`, the skill's `--webapp` base]→bounded poll→findings by type; **share view unchanged**). Consumer = er-handoff-memo skill **v2.3.0** `cat_` branch (§9 manifest→page→findings; `handoff.py` `page`/`findings` subcommands). Spec/plan: `docs/superpowers/archive/specs/2026-07-11-c12-cat-content-audit-bridge-design.md` + `archive/plans/2026-07-11-c12-cat-content-audit-bridge.md`
- `lib/ada-audit/seo/discovery-coverage.ts` — C6 hybrid-discovery Increment 1 pure sitemap miss-rate: `normalizeCoverageUrl` (strips fragment + UTM + non-root trailing slash + `www.` + pins scheme — MUST match `link-harvest` www-insensitivity or false misses) + `computeDiscoveryCoverage({discoveredUrls, internalLinks, discoveryMode, discoveryCapped})` → diffs coverage-normalized internal-link targets (images + non-page extensions excluded) vs the coverage-normalized `SiteAudit.discoveredUrls` baseline; `missRate=|O|/(|B|+|O|)`; `applicable = mode==='sitemap' && !capped`. ZERO fetches; NOT a Finding (stored on `CrawlRun.discoveryCoverageJson`). Called in the builder from already-loaded rows
- `lib/ada-audit/broken-link-recovery.ts` — C6 `recoverBrokenLinkVerifies()`: re-enqueues verifiers stranded by the fire-and-forget crash window (complete audit + (**`HarvestedLink` OR `HarvestedPageSeo`**) rows + no verify job + no live-scan run); awaits a real enqueue; in `recoverQueue()` (boot) + `stale-audit-reset` (10-min)
- `components/site-audit/BrokenLinksSection.tsx` — C6 results-page block (not-verified / verified-clean / findings states) reading the live-scan run, **scoped to `broken_*` types only** (C6 Phase 2: the run now also holds on-page findings)
- `components/site-audit/OnPageSeoSection.tsx` — C6 Phase 2 results-page block reading the SAME live-scan run, scoped to the on-page types; states: not-analyzed / pre-Phase-2 (`analyzed` probe = run has a `CrawlPage` with `statusCode != null`) / clean / findings. "Clean" = no on-page findings among successfully-audited HTML pages (NOT whole-site)
- `lib/sales/` — C14 prospect sales view server layer: `sales-report-data.ts` (`loadSalesReportData(token)` → `invalid`/`pending`/`ready`; `validateSalesToken`; `curatedScreenshotSet(prospectId, adaAuditId)` — the screenshot allowlist, built from the SAME `topPatternIssues` selection so it can't drift wider; latest-REPORTABLE resolution = complete + seoOnly:false + has a `seo-parser` run. **C14 urgency redesign (PR #169) payload v2:** `overallScore` (rounded avg of AVAILABLE headline values — ada/seo/perf/schema — nulls EXCLUDED from the denominator, never zeroed), `heroScreenshot` (= `homepageScreenshot != null`), `standardTested` (via `standardLabel`), per-`SeoIssueGroup` `affectedPages` (distinct page-scope finding URLs — the ONLY value that may drive an urgency bar; `count` stays the heterogeneous targets/groups unit) + `affectedComplete`, `performance: {rollup, homepage}` where `homepage` (via `pickHomepageCwv`) is INDEPENDENT of the <3-page rollup-null. NOTE: `topPatternIssues`/`loadRepresentativeExamples`/`curatedScreenshotSet` are RETAINED for the still-live screenshot route but the redesigned report no longer renders any `/screenshot/` URL — that public route is now orphaned-but-authorized (ownership+curated-set gated, non-guessable; retire/tighten candidate)), `cwv-aggregate.ts` (pure Lighthouse lab roll-up, null <3 pages; `worstPages` cap 5; `pickHomepageCwv` = deterministic canonical-root-then-(url,id) homepage CWV, rollup-independent), `root-url.ts` (**C14 hero** pure client-safe `isRootUrl`/`canonicalRootUrl`/`injectProspectRoot` — prospect-only discovery root injection so the homepage is always audited), `hero-screenshot.ts` (**C14 hero** `HERO_SCREENSHOTS_DIR` file store — atomic UNIQUE-temp+rename, ENOENT-tolerant delete; outlives the 24h screenshot sweep for 30-d tokens; mirrors `REPORTS_DIR`), `representative-examples.ts` (bounded one-child-audit evidence loader, dedup-by-html, cap, archived fallback — never fans out), `copy.ts` (ALL canned persuasion copy — `ISSUE_WHY`/`SCHEMA_IMPLICATIONS`/`SCORE_METHOD`/`WCAG_MEANING`/`ER_ADA_CTA`/`standardLabel`; honest labels, no prospect compliance/CWV-pass claims; `ER_ADA_CTA` is the ONE sanctioned ER-product ADA claim; schema copy evidence-bounded, never "required")
- **C14 hero capture pipeline (PR #169):** additive `SiteAudit.homepageScreenshot` (filename under `HERO_SCREENSHOTS_DIR`, stamped ONLY after a successful file write, prospect audits only; migration `20260715000000`). The ADA runner (`runner.ts`) `captureHeroScreenshot` grabs a viewport PNG via the already-held puppeteer page on the audited path AND on RENDERED same-domain-root redirects (gate `rendered && isRootUrl(finalUrl, parsed.hostname)` — root→www is the common prospect case); capture failure logs + never fails the page. `site-audit-page.ts` `publishHeroScreenshot` is FENCED to the winning settle (both audited + redirected paths), stamps guarded `where:{id, prospectId:{not:null}}`, deletes the file on count-0/throw (orphan guard). Deletion at 3 seams: SiteAudit DELETE (additive to `reportCleanup`), prospect DELETE (snapshot ALL linked audit ids BEFORE delete — incl. unstamped files — then null column + best-effort delete), scheduled retention flatMap. Public `GET /api/sales/[token]/hero/[siteAuditId]` (indistinguishable-404 incl. ENOENT, non-oracle 500 on non-ENOENT fs error, ownership-equality scoping) behind anchored single-segment matcher `^/api/sales/[^/]+/hero/[^/]+$`. `HERO_SCREENSHOTS_DIR` env = `${DATA_HOME}/sales-hero` (optional-with-default `<cwd>/data/sales-hero`).
- `components/sales/` — C14 public report UI (server components; **urgency redesign PR #169**): `SalesReportView` assembles `SalesReportHeader` (sticky/shrink, Book-a-review→`#inquiry`) + `HeroRow` (homepage screenshot card + animated `ScoreGauge`, slot hidden when `heroScreenshot=false`) + `HeroTiles` (mini `UrgencyBar` per metric) + rebuilt `sections.tsx` (Accessibility counts-only · SEO `affectedPages`-driven urgency rows + `ISSUE_WHY` · Performance homepage-CWV+slowest+rollup · Structured-data evidence-bounded 2×2 grid; all `defaultOpen`) + `InquiryForm` (mailto placeholder). Client leaves `ScoreGauge`/`UrgencyBar`/`InquiryForm`/`SalesReportHeader`; score-methodology behind PR-1 `Explainer` (ONLY static prose — operational truth stays visible). `ExampleCard` REMOVED; `SectionCard` gained `defaultOpen` (lost `intro`). `intake/ProspectDashboard.tsx` = the ONE intake client component (form + prospect list + 8s smart polling). **Sales-overhaul PR3 (PR #170):** each card now renders a phase-labeled weighted progress bar + `startedAt`-based ETA (pure `intake/progress-math.ts` — 70/15/15, `pagesRedirected` settled, reserved PDF/LH weights until pages drain so it's monotone; ETA on a hydration-safe post-mount 1s tick that NEVER fetches; poll/SSE cadence unchanged) and is a whole-card `role="link"` click-through to the public report in a new tab (`opener` nulled by hand, NEVER the `noopener` feature; sync `window.open` + about:blank pre-open + share-mint for tokenless; `closest()` guard + per-button `stopPropagation` protect nested controls). `listProspects` carries progress counters, `startedAt`, `queuePosition` (via `queue-order.ts`), and row `salesUrl` (via the single-home `buildProspectSalesUrl` in `lib/services/prospects.ts`)
- `lib/services/prospects.ts` — C14 `createProspect` (best-effort app-level domain dedupe, no DB unique — client-schedules precedent), `listProspects` (latest-audit join + `reportable` flag), `normalizeProspectDomain`
- `lib/ada-audit/seo/schema-types.ts` — C14 pure `aggregateSchemaTypes` → versioned `CrawlRun.schemaTypesJson` `{v,observedPages,pagesWithSchema,types[]}` histogram; computed in the live-scan builder before transient deletion (benefits ALL audits)
- `app/api/sales/` — C14 routes: cookie-gated `prospects` (create/list), `prospects/[id]` (DELETE→SetNull), `prospects/[id]/scan` (full audit), `prospects/[id]/share` (POST rotate-or-extend / GET read-only); PUBLIC token-validated `[token]/screenshot/[adaAuditId]/[filename]` (ownership chain + curated-set membership)
- `components/ada-audit/SiteAuditResultsShell.tsx` — C18 results shell: shared header (domain, ADA + SEO score rings, `SiteAuditExportBar`, diff panel) + **Accessibility | SEO tabs** (`?resultTab=` URL sync) wrapping the accessibility results and the SEO section stack; the share page renders the same shell with ALL SEO-tab data loaded server-side token-validated (shareMode omits pattern screenshots/element dropdowns — the screenshot route stays cookie-gated)
- `app/api/site-audit/[id]/pattern-sample/route.ts` — C18 bounded pattern-sample loader for the expandable `CommonIssueCallout` cards: resolves the site-wide pattern's representative page and loads that ONE child audit's stored results for the rule (never fans out across children; 90-d-archived audits degrade to the capped ~5-node no-image sample)
- `lib/ada-audit/site-audit-helpers.ts` — `buildSiteAuditSummary()`, `addScorecards()`, `ZERO_SCORECARD`
- `lib/services/scoring.service.ts` — `computeHealthScore()` for SEO parser crawls (8 weighted factors over `lib/scoring/weights.ts`; C8 levers = `ScoringWeights` DB singleton + `/settings` card; explanations = persisted `scoreBreakdown` → `components/scoring/ScoreExplanation.tsx`)
- `lib/ada-audit/scoring-v2.ts` — ADA score v2 formula (saturating severity-weighted violation DENSITY; constants hardcoded, no levers). **`ADA_SCORE_VERSION` is 3 since C13 (2026-07-09)** — same formula, repaired input: the axe `no-passes` reporter had silently stripped `incomplete` (and `passes`) from every stored blob, so the incomplete penalty NEVER fired pre-v3; cross-version sparkline deltas are suppressed (`buildSeries.formulaChanged`)
- `lib/ada-audit/axe-trim.ts` — C13 `trimAxeResultsForStorage`: string-injected IN-PAGE (same SWC-helper-free contract as `parse-seo-dom.ts`) after `axe.run` (reporter `'v2'`) — stores the `passCount` scalar, deletes `passes`/`inapplicable` before CDP serialization, keeps `incomplete` (node-capped in Node). Blob shapes: legacy = violations only (counts read as 0); post-C13 = violations + incomplete + `passCount`; readers prefer the scalar
- `lib/keywords/` — KS-1 (C20) GSC keyword-snapshot layer: `types.ts` (client-safe constants + `GscSnapshotSummary`; re-exports the provider row types — never redeclares), `window.ts` (pure UTC trailing-91-day window ending 3 days back), `derive.ts` (pure signals — raw-decimal bands wins ≤10 / opportunities >10–≤30 / quick wins >10–≤20, position ≤0 DISCARDED here but kept at storage; cannibalization = ≥2 pages each ≥20% share of the **observed** query×page impressions + ≥10 imp; `observedPageCoverage` nullable, NOT clamped ≤1), `gsc-snapshot.ts` (service: synchronous single-flight Map — the derived `.finally()` cleanup chain carries its own no-op `.catch`, an unhandled rejection there crashes the process; validate→derive→create atomic publish; reads filter on the row's verbatim `gscSiteUrl` stamp vs the CURRENT client mapping, `fetchedAt DESC, id DESC`, corrupt-newest falls back to next valid; summary lists capped 50/50/50/20 HERE, counts stay full), `retention.ts` (keep-latest-3 per client, tagged `$executeRaw`, in `runCleanup`). Provider seam: `fetchGscQueryPage()` in `gsc-provider.ts` (own result union — `not_mapped` ≠ `access_denied`; `fetchGsc`/`SourceResult` untouched). Routes `GET/POST /api/clients/[id]/gsc-snapshot` (cookie-gated, withRoute); UI `components/clients/GscKeywordCard.tsx`. Absence is ALWAYS "not observed in this GSC window", never "not ranking"; `…AtLimit` means "possibly truncated", never definite. Spec: `docs/superpowers/archive/specs/2026-07-10-ks1-gsc-query-snapshot-design.md`. **C12 Increment A (cannibalization report) reuses this layer:** `gsc-snapshot.ts` extracted a shared private `loadLatestValidSnapshot(clientId)` (newest-valid resolver: unknown client `clientExists:false` ≠ unmapped `gscMapped:false` ≠ no-usable-snapshot; corrupt-newest falls through) — `getLatestGscSnapshot` now DELEGATES to it (behavior-preserving) and `getCannibalizationReport(clientId)` re-derives the FULL uncapped `derive.ts` cannibalization list at read time (payload capped at `CANNIBALIZATION_REPORT_CAP`=200 with `totalCannibalizedQueries` reporting the full count; both `queryAtLimit`/`queryPageAtLimit` + `capped` surfaced as "possibly truncated"). Route `GET /api/clients/[id]/gsc-cannibalization` (cookie-gated, strict `^[1-9][0-9]*$` id, 404 ONLY on `clientExists:false`, NO middleware change); UI `components/clients/GscCannibalizationCard.tsx` (after `GscKeywordCard`; independent Refresh POSTs the snapshot then GETs the report). Spec/plan: `archive/{specs,plans}/2026-07-11-c12-tier0-content-signals*`. **KS-2 volume layer (dark, NO consumer until KS-5):** `volume-config.ts` (call-time env gate `DATAFORSEO_LOGIN`/`DATAFORSEO_PASSWORD` — missing/empty = feature disabled, NEVER a boot check; fixed `api.dataforseo.com` host, no function accepts a URL), `volume-normalize.ts` (the ONE shared canonicalizer for cache keys AND transport response matching — never reimplement), `volume-throttle.ts` (process-wide rolling 12-req/60s singleton; grant-time stamping; tail recovers from rejected sleeps — same derived-promise crash class as the snapshot single-flight), `dataforseo-client.ts` (Google Ads `search_volume/live`; per-request-key outcomes `returned|not_returned` — a spell/similar-grouped response item is NEVER remapped onto the requested keyword; empty result array = valid all-not_returned, missing/null result = `unparseable_response` and never negative-cached), `volume.ts` (`getKeywordVolumes(keywords, locale)`: cache-first vs `KeywordVolumeCache` (unique `[keyword,locationCode,languageCode,providerVersion]`, 30-d TTL, negative caching, per-key upserts only), ≤500-key IN-batch reads, chunk ≤1000 cap 3/invocation, accounting incl. `providerCost` on OK AND ERROR — 0 when nothing was sent, null only when genuinely unresolved), `retention.ts` prune in `runCleanup`. Spec: `archive/specs/2026-07-10-ks2-dataforseo-volume-provider-design.md`. **KS-3 client keyword profile (consumer = KS-5):** `Client` carries `institutionType` ('trade'|'bootcamp'|'university'|'k12'|'other'), `programsJson` (confirmed `ProgramEntry[]` roster), `programSuggestionsJson` (suggestions + provenance + dismissedNames), `kwLocationCode`/`kwLanguageCode`/`kwMarketLabel` (primary DataForSEO locale). `program-roster.ts` (client-safe types + ONE shared `checkEntryFields` gate backing write validation AND the strict sanitizing read parsers — read is exactly as strict as write, never loosen one side), `locales.ts` (curated 6-market list + `validateProfileLocale` — bare two-letter language codes ONLY, stricter than `normalizeLocale` on purpose: DataForSEO's case-sensitivity for lowercased regionals (`zh-tw`) is UNVERIFIED and the canonicalizer lowercases before the wire; `normalizeLocale` itself is the Google Ads provider seam and stays untouched — a Labs endpoint gets its own validator), `program-suggest.ts` (pure `deriveProgramSuggestions`: pillar `classifyPageType` slug signal + JSON-LD program entities, evidence-ranked, confirmed/dismissed excluded, names capped 200 so confirm can't bypass `validatePrograms`). Scan side: `parse-seo-dom.ts` extracts JSON-LD Course/EducationalOccupationalProgram names (injected-code contract: `String(v)===v`, never typeof) → transient `detailsJson.programNames` → `lib/ada-audit/seo/program-entities.ts` `aggregateProgramEntities` (deterministic smallest-URL winner) → durable `CrawlRun.programEntitiesJson`, written by the live-scan builder pre-deletion, fail-to-null, caller-filtered `indexableOf ∧ ¬loginLike`; historical runs lack it — suggestions degrade to slug/heading. Service `lib/services/keyword-profile.ts` (`hasLiveScan`, documented last-writer-wins + the UI refetches the whole profile after EVERY mutation, archived-client guards on mutate AND suggest); routes `GET/PATCH /api/clients/[id]/keyword-profile` + `POST …/suggest` (cookie-gated, `conflicting_ops` matrix, NO middleware change); UI `components/clients/KeywordProfileCard.tsx`. Spec: `archive/specs/2026-07-10-ks3-client-profile-roster-design.md`. **KS-4 FAQ tri-state + page inventory (dark, consumer = KS-5):** per-page FAQ evidence on `CrawlPage.faqEvidence` (nullable TEXT; grammar `present:<sig,list>` canonical order schema,heading,container,questions | `not-detected` | NULL = unknown — **detection proves presence, never absence; a malformed/legacy shape must NEVER read as not-detected**). Chain: `parse-seo-dom.ts` bounded in-page `faqSignals` (300-eligible/600-raw h2-h4 caps, boilerplate+hidden guards, faq-id/class container needs a heading/`<details>` descendant OR is itself `<details>`, questionHeadings = '?'-ended count; injected contract held) → `persistPageSeo` `detailsJson.faqSignals` → pure `lib/ada-audit/seo/faq-evidence.ts` `deriveFaqEvidence(detailsJson)` (the ONE home of the presence rule: schema accepts URI forms `https://schema.org/FAQPage`; questions fires at ≥3; a NEGATIVE requires EVERY field well-formed, positives fire past corrupt neighbors) → builder `ensurePage` scalar (null-skipping merge keeps the unknown default) → `lib/keywords/page-inventory.ts` (KS-5's assembly seam: strict-grammar `parseFaqEvidence` — anything off-canon decodes `unknown` — + pure `buildPageInventory`: `indexable===true` filter, url-sorted, read-time `classifyPageType(url, [], crawlDepth)` with `programEntityUrls` upgrade to program/0.7 ONLY when confidence ≤0.4 — explicit slug classifications never overridden; entity URLs `new URL`-validated + `normalizeFindingUrl`-matched). `CrawlPageInput.faqEvidence` is REQUIRED — every producer takes a position (seo/ada mappers: null). NOTE: tsconfig excludes `**/*.test.ts` — tsc never flags test fixtures. Migration `20260711000000`. Export phrasing (KS-5): not-detected = "no FAQ detected — verify", never "confirmed no FAQ". Spec: `archive/specs/2026-07-10-ks4-faq-tristate-page-inventory-design.md`. **KS-5 client-scoped keyword-strategy export (C20 capstone; first consumer of KS-2 + KS-4):** NEW `kst_` token family `lib/keyword-strategy-token.ts` (audience `keyword-strategy-client`, scopes read/memo-write/**volume-lookup**, SAME env `KEYWORD_MEMO_TOKEN_SECRET` as krt_ — the AUDIENCE is the isolation wall; volume-lookup is NEVER on memo tokens). Models: `KeywordStrategySession` (client-FK cascade, one row PER MINT, `volumeKeywordsUsed` counter + cap stamped at mint from `VOLUME_SESSION_KEYWORD_CAP` default 1500) + `KeywordStrategyVolumeRequest` (unique `[strategySessionId,idempotencyKey]`, state `reserved|settled|unresolved`, `responseJson` replay). `strategy-export.ts` `loadStrategyExport(sessionId)` — five INDEPENDENTLY-degradable blocks (KS-3 profile · KS-1 gsc · KS-4 inventory over the newest live-scan run, KS-3 precedent NO seoIntent filter · run-scope findings split by `lib/findings/finding-type-sets.ts` — the ONE home of the on-page/broken type lists, the results-page sections import it too · optional semrush via `buildKeywordResearchExport`); corrupt block → null, never throws. `strategy-volume-ledger.ts` — the money core: reserve = array-form txn `INSERT..SELECT` (cap predicate) + counter UPDATE fenced on cap AND `EXISTS(request row reserved)`, `(1,1)` contract, duplicate probe BEFORE budget verdict (settled → 200 replay of stored responseJson, else 409); settle refund derives from the STORED `keywordCount` clamped in SQL (caller numbers never control refunds), EXISTS-fence-before-flip = exactly-once, throw/poisoned-cost → `unresolved` holds the FULL reservation; monthly ceiling `used+n<=VOLUME_MONTHLY_KEYWORD_CEILING` (default 25000) on request-row `createdAt` UTC month; `sweepStaleReservations` (>24h reserved → unresolved, NO refund) + TIERED retention in `runCleanup` (memo-less+no-requests 7d / memo-less+requests 45d — never deletes rows the monthly SUM counts / memo-bearing kept forever). Routes: cookie-gated mint `POST /api/clients/[id]/keyword-strategy/mint-token` (best-effort `refreshGscSnapshot` first — Q6 refresh-on-mint; mint-throw deletes the orphan row) + poll GET; PUBLIC `GET /api/keyword-strategy/[id]` / `PATCH …/memo` / `POST …/volumes` (body-before-auth → auth → dark gate → locale FIXED server-side from the profile (body locale ignored) → route-filter via imported KS-2 constants → monthly precheck → reserve → try IMMEDIATELY after reserve → `getKeywordVolumes` → finally-settle `.catch(logError)`; ≤300 kw/request = one self-limiting chunk, no route abort) — exactly 3 anchored middleware regexes. UI `components/clients/KeywordStrategyCard.tsx` (after KeywordProfileCard; `Strategy ID:` + `kst_` clipboard via `lib/keyword-strategy-prompt.ts`; memo-poller reused; regenerate anchors baseline to NULL so the old memo survives until the new write-back). Skill: er-handoff-memo v2.2.0 kst_ branch + `handoff.py volumes` (skill routing is a RELEASE PREREQUISITE before any deploy exposing the card). Volumes dark until `DATAFORSEO_LOGIN`/`PASSWORD` in prod .env (honest 409 `volume_disabled`). Migration `20260711120000`. Spec: `archive/specs/2026-07-11-ks5-keyword-strategy-export-design.md`
- `lib/seo-fetch/` — D3 shared robots/sitemap layer, ONE home ending the 3-robots-parsers/2-sitemap-parsers drift: `robots-parse.ts` (client-safe rich validator parser: `parseRobotsTxt` 23 issue types/AI-bot audit/`testUrlAgainstRobots`, + shared comment-stripping `extractSitemapUrls`), `robots-match.ts` (client-safe MINIMAL crawl-frontier matcher `parseRobots`/`isAllowed` — star-group-only, $-aware; intentionally distinct from the validator matcher, never unify), `sitemap-parse.ts` (client-safe `parseSitemapXml` validation + crawl helpers `isSitemapIndex`/`extractPageLocs`/`extractChildSitemapLocs`), `fetch.ts` (server-only primitives over `safeFetch` — NEVER a raw fetch: `fetchRobotsTxt` (`new URL('/robots.txt', base)` contract), `fetchSitemapXml` (gzip, 5 MB cap, HTML-content-type reject), `collectSitemapPageUrls` (ONE-level index expansion frozen, injected fetcher = crawler's browser fallback, `childrenTotal/childrenFailed` diagnostics); discriminated-union `SeoFetchResult` with failure taxonomy backed by `SafeUrlError.reason` ('policy'|'dns'|'redirect'|'invalid-response', additive — the SSRF guard's throw behavior/messages byte-identical)). Consumers: robots-validator page (client-side parse), `sitemap-crawler.ts` (adapters preserve ''/null-on-failure incl. empty-200-body→browser-fallback), future D4/D5 checks. D6 micro-delta (2026-07-12): `Sitemap:` extraction strips #-comments
- `lib/robots-check/` — D4 client-attached robots/sitemap checks: `runner.ts` (server-side check over `lib/seo-fetch` — DI fetchers, honest caps `sitemapsSkipped`/`childrenSkipped`/`childrenExcluded`/`timeBudgetExhausted`, per-child hash observations + `childrenHash` for index-churn change evidence, convention-probe recognition gate, 60s soft budget + one 15s overshoot window), `service.ts` (single-flight per client:domain persist — gsc-snapshot crash-lesson pattern; read-time `changed` vs predecessor by robotsStatus+robots hash+ordered sitemap (url,hash,childrenHash) triples — NEVER persisted, D5 refines without backfill; `getRobotsCheck` = the ONE summary+detail shape), `retention.ts` (keep LIMIT+1 per (client,domain) — hidden predecessor keeps the oldest visible `changed` stable; in `runCleanup`), `types.ts` (client-safe). Routes `GET/POST /api/clients/[id]/robots-checks` + `GET …/[checkId]` (cookie-gated, NO middleware change); UI `components/clients/RobotsCheckCard.tsx`. Raw robots body stored on `RobotsCheck.robotsContent` (D5 diffs); sitemap XML NEVER stored (hash+counts only). Only client-registered domains get rows; anonymous validator-page runs never persist. Migration `20260713100000`. Spec: `docs/superpowers/specs/2026-07-12-d4-client-robots-checks-design.md`. **D5 scheduled monitoring + change-only alerts:** weekly `system-robots-monitor` schedule (`weekly:1@06:30` server-local) → `robots-monitor-sweep` fans out one `robots-monitor` job per (active client, `normalizeClientDomain`-normalized registered domain; dedupKey `robots-monitor:<clientId>:<domain>`), each payload carrying `slotStartedAt` = the SWEEP job's createdAt — the durable reuse boundary (a retry or re-enqueued child reuses the slot's existing scheduled RobotsCheck row instead of refetching; a prior week's row never reuses). Handler order is load-bearing: revalidate (archived/delisted; NORMALIZED stored-domain membership) BEFORE reuse/fetch/alert → fresh runs source-fence (a manual single-flight winner absorbs the change silently — manual checks NEVER alert) → resolve via `getRobotsCheck` (`StoredRobotsCheck` now carries `changeSummary` from client-safe pure `change-summary.ts`: multiset robots line diff capped 50 lines/200 chars, (url,ordinal)-paired sitemap deltas, `robotsContentChanged`/`orderChanged` honesty flags; NULL diff = "line diff unavailable" vs non-null EMPTY = "reordering or formatting only" — never conflate) → alert ONLY on `changed === true`: read `RobotsCheck.alertSentAt` marker → dark gate (notify env dark = PERMANENT suppression, no stamp, no catch-up) → `buildRobotsChangeEmail` (`lib/notify/robots-change-content.ts`, transport-honest wording — "could not be fetched (timeout)", never "removed") to `notifyAdminEmail()` → conditional stamp (at-least-once, D7 pattern). Card renders the same changeSummary as "Changed vs previous" + the childrenExcluded count. Migration `20260713120000` (alertSentAt). Spec: `docs/superpowers/specs/2026-07-13-d5-scheduled-robots-monitoring-design.md`
- `lib/handoff/` — D1 six-family skill-handoff engine (registry/meta/errors/token factory/prompt/route-auth) consolidating pat_/srt_/krt_/kst_/cat_/qct_; the six `lib/*-token.ts` + `lib/*-prompt.ts` modules and both legacy route-auth helpers are now thin facades over it; characterization suites in `lib/handoff/*.test.ts` are the frozen-wire gate (audience string literals like `'pillar-analysis-narrative'` never change even after the legacy skill dir was retired 2026-07-13)
- `components/handoff/` — D1 `useMemoPoller` hook + `MemoHandoffCard`; the srt/krt dashboard cards are thin wrappers, the pillar `MemoPoller` and the kst card adopt the hook directly
- `lib/parsers/` — CSV parsers extending `BaseParser` with O(1) `headerMap` column lookup

## Architecture patterns
- **ADA audit polling:** POST creates record + enqueues a durable `ada-audit` job (dedup/group key `ada-audit:<id>`; enqueue failure flips the row to error + 500) → the job handler (`lib/jobs/handlers/ada-audit.ts`) updates `progress`/`progressMessage` as it runs (every write fenced by `status='running'` — first terminal writer wins) → client polls `/api/ada-audit/[id]` every second → `AuditPoller` renders live progress bar with elapsed + estimated time. Standalone concurrency = `ADA_AUDIT_CONCURRENCY` (default 2). Orphaned standalone audits/PDF rows (zero active jobs in their `ada-audit:<id>` group, >5 min old — AdaAudit has no `updatedAt`; job state is the liveness source) are failed by `lib/ada-audit/standalone-recovery.ts`, called from `resetStaleAudits()` + `recoverQueue()`
- **Site audit queue:** Only one site audit runs at a time, enforced at the DB level by the `site-audit-discover` job's conditional claim (`NOT EXISTS` over transient statuses). `enqueueAudit()` creates the record in `queued` status → `processNext()` is a stateless promoter (no mutex): when nothing is transient it enqueues a `site-audit-discover` job (dedupKey `discover:<id>`) for the first-ranked queued audit under the shared total ordering (`lib/ada-audit/queue-order.ts` — prospect-owned scans jump the line; PR3); `finalizeSiteAudit` kicks it on completion. `SiteAuditPoller` shows queue position + active audit progress. `SiteAuditForm` polls `/api/site-audit/queue` every 5s to show a live banner. `SiteAuditHistory` smart-polls every 8s when active audits exist.
- **Site-audit phase model:** `queued → running → (pdfs-running | lighthouse-running, in that priority) → complete`. The `site-audit-discover` job claims `queued→running`, discovers pages, persists `discoveredUrls`+`pagesTotal` (the finalizer's discovery guard: a `running` audit with null `discoveredUrls` is never finalized), creates child rows, and fans out one `site-audit-page` job per URL. Each page job runs axe, dispatches PDFs FIRST (pdfsTotal-before-pagesComplete invariant), settles child + counters in one array-form transaction, then enqueues PSI (`lib/ada-audit/lighthouse-queue.ts`). `finalizeSiteAudit` is the single decision point: it requires `pagesDone && pdfsDone && lighthouseDone` to flip to `complete`, otherwise picks `pdfs-running` (priority) or `lighthouse-running` as the transient status; it reads scalars only and loads children once, at completion. PSI concurrency is `PSI_CONCURRENCY` (default 6); page concurrency is `SITE_AUDIT_CONCURRENCY` (default 1, prod 2). **C11 PR1 `seoOnly` render-only mode:** a `SiteAudit.seoOnly` audit (set by `POST /api/site-audit {seoOnly:true}` — the Site Audit form's Scan Type = SEO (C16: `SeoScanForm` retired) — or a seoOnly schedule (C11 PR 2a); `seoOnly⇒seoIntent` forced at `queueSiteAuditRequest`) takes a render-only page path (`runAxeAudit({renderOnly})`→`kind:'rendered'`: nav+settle+harvest only; NO axe/screenshots/PDF-dispatch/PSI), so pages settle straight to `complete` with `pdfsTotal===0 && lighthouseTotal===0`. The page job reads `seoOnly` off the PARENT row (authoritative, not the payload); the finalizer writes a null `summary` and **skips ALL ADA output — NO `tool:'ada-audit'` CrawlRun, no carry-forward** (ADA report/csv/vpat gate on the ada run existing → they 409 `no_findings_run` for seoOnly), but still fires `enqueueBrokenLinkVerify` (the live-scan builder — the ONLY output; result at `/seo-audits/results/run/[liveScanRunId]`, readiness signalled by `liveScanRunId` on `GET /api/site-audit/[id]`). **C16:** `/ada-audit/site/[id]` OWNS seoOnly routing — transient → `SiteAuditPoller`; complete → server redirect to the run results page (or `SeoPhaseBanner` while the verifier builds), branch placed BEFORE the ADA summary resolution (`seo-only-view.ts`); all producers link the site page (only surfaces with the run id in hand — unified recents, `ScheduledScansCard` — link the run page directly); unified recents badges seoOnly rows “Site SEO”.
- **Scheduled scans (C2):** client scan schedules are plain `Schedule` rows (`name: null`, `clientId` set, `jobType: 'scheduled-site-audit'`, payload `{clientId, domain, wcagLevel}`) managed via `GET/POST /api/clients/[id]/schedules` + `PATCH/DELETE …/[scheduleId]` (cookie-gated; cadence must be literal `weekly:`/`monthly:` — `daily@`/`every:*` are 400 `cadence_not_allowed` until C3 makes blobs prunable; one schedule per (client,domain) is best-effort app-level; resume recomputes `nextRunAt` and 409s on archived clients; DELETE cancels the `schedule:<id>` job group and SetNulls historical audits to manual-class). The tick fires the wrapper job → `queueSiteAuditRequest()` → SiteAudit born with `scheduleId` + `requestedBy: 'scheduled'`; a duplicate in-flight audit consumes the slot (no catch-up). `monthly:<1-28>@HH:MM` cadence + `cadenceClass()` live in `lib/jobs/scheduler.ts`. UI: `ScheduledScansCard` on `/clients/[id]` (create/pause/delete, last-run score + Δ), timeline titles get " · scheduled". Retention: `pruneScheduledSiteAudits()` hard-deletes schedule-originated terminal audits past per-cadence windows — manual and orphaned (schedule-deleted) audits are NEVER pruned.
- **Stale audit recovery:** `updatedAt` auto-updates on every Prisma write (raw-SQL counter bumps set it manually) — that's the heartbeat. Both `resetStaleAudits()` (every 10 min via the `stale-audit-reset` scheduled job, 5-min threshold) and `recoverQueue()` (once at startup) apply ONE generic treatment to all transient parents (`running` included since Phase 3): outstanding durable jobs in `site-audit:<id>` → resume; zero jobs → one finalize attempt (drained-but-unfinalized completes); still transient → `failSiteAudit()`, the shared destructive path (conditional parent flip that never clobbers terminal rows, then `failOrphanAdaAudits` + `failOrphanPdfAudits` + `cancelJobsByGroup` + batch close + promoter kick). A failed job count never destroys a parent — skip and retry next pass.
- **Durable job queue:** `Job` + `Schedule` tables (`lib/jobs/`) with a single in-process worker — conditional-update claim, attempt-fenced heartbeat/settle, per-type concurrency/timeout/backoff, `onExhausted` domain hooks, and a 60s schedule tick (exactly-once-per-slot via `@@unique([scheduleId, scheduledFor])`). PSI, PDF scans, the site-audit page loop, and standalone single-page ADA audits all run through it unconditionally (`psi`, `pdf-scan`, `site-audit-page`, `site-audit-discover`, `ada-audit` job types; the legacy in-memory PSI pool, `pdf-worker-pool.ts`, the in-memory page loop + `processing` mutex, and the route's fire-and-forget `runAuditInBackground` are deleted) — `running`, `pdfs-running`, and `lighthouse-running` site audits and in-flight standalone audits all survive restarts and resume draining. Recurring maintenance (daily `runCleanup`, 10-min `resetStaleAudits`, 30-min screenshot sweep) runs as scheduled jobs seeded from `lib/jobs/system-schedules.ts` — `instrumentation.ts` owns no `setInterval`s (startup `runCleanup()` stays inline). Terminal Job rows are pruned by `lib/jobs/retention.ts` (complete/cancelled 7 d, error 30 d, slot-record guard). PDF scan concurrency = `PDF_POOL_SIZE` (default 4). Specs: `docs/superpowers/archive/specs/2026-06-10-durable-job-queue-design.md` + `2026-06-10-durable-job-queue-phase3-design.md` + `2026-06-10-durable-job-queue-phase4-design.md`.
- **Findings layer (A2):** every completed parse, site audit, and standalone ADA audit dual-writes a normalized `CrawlRun` → `CrawlPage`/`Finding`/`Violation` subtree (origin FKs `SetNull`, subtree cascades from `CrawlRun` only; never backfill historical blobs). Hooks are fire-and-forget AFTER the legacy commit (`void write…().catch(log)` — the finalizer hook stays LAST in `finalizeSiteAudit`); a dual-write failure logs `[findings] dual-write failed` and the fix is `npx tsx scripts/findings-rebuild.ts <id>`. The seo-parser pages route reads `CrawlPage` + `Finding` join with a verbatim `SessionPage` fallback for pre-A2 sessions (`SessionPage` is no longer written; model drop ≥180 d after 2026-06-11). Blob retention: `pruneArchivedBlobs()` in `runCleanup()` nulls blobs 90 d after completion — `PRUNE_ACTIVATED['ada-audit']` is ACTIVE (C3): origin `SiteAudit.summary`/standalone `AdaAudit.result` AND site-audit child `AdaAudit.result` blobs are nulled (child `lighthouseSummary` kept) with snapshot-based screenshot-artifact deletion; every ADA read surface serves a findings-table fallback when the blob is null (scores prefer `CrawlRun.score` everywhere; detail views degrade via `findings-fallback.ts` with an archived banner). `'seo-parser'` is ACTIVE since C5: `Session.result` nulled at 90 d — results/share/parse-GET/format-exports serve the degraded findings fallback (`seo-findings-fallback.ts`, archived banner); diff and the claude/srt_/krt_ memo exports refuse with 409 `session_archived` (only when the `archivePrunedAt` stamp exists — a null blob without it keeps legacy errors); history reads `CrawlRun.score` + `Session.totalUrls` (blob only pre-A2); pre-A2 sessions (no CrawlRun) are never pruned. Spec: `docs/superpowers/archive/specs/2026-06-10-findings-layer-design.md` (+ C5 `2026-06-12-seo-ingestion-interface-design.md`).
- **Broken-link verifier (C6):** the ADA site-audit page job harvests `<a href>`/`<img src>` targets (`link-harvest.ts`) and persists them to the transient `HarvestedLink` table **after a successful page settle** (fenced — a zombie attempt that lost the flip writes none; chunked at 50). When the audit reaches terminal `complete`, `finalizeSiteAudit` fire-and-forget enqueues `broken-link-verify` (LAST). That job dedupes harvested targets (deterministic `orderBy` so the cap is stable), verifies each same-domain internal-link/image via `safeFetch` (HEAD→GET precision) under bounded concurrency + per-host throttle, writes a **live-scan `CrawlRun`** (`source:'live-scan'`, `tool:'seo-parser'`, `score:null`) via `writeFindingsRun` — coexisting with the ada-audit run via the C6 compound unique — then deletes the HarvestedLink rows. `pruneHarvestedLinks()` (7-d) backstops stranded rows; `recoverBrokenLinkVerifies()` re-enqueues stranded verifiers. **Source-aware selection:** `findings-shared.selectRuns` keeps `sf-upload` as the canonical SEO score/health run and exposes `seo.liveScan` separately — a live-scan run NEVER displaces the score (B1 series + client-findings exclude/segregate it); broken-link findings surface additively in the B2 panel + the results-page `BrokenLinksSection`. `pruneArchivedBlobs` is tool-origin-aware (seo-parser prunes only session-origin `Session.result` — a live-scan run has no blob and must never null the ADA `SiteAudit.summary`). Spec/plan: `docs/superpowers/archive/specs/2026-06-16-broken-link-verifier-design.md` + `archive/plans/2026-06-16-broken-link-verifier.md`. **C6 Phase 2 (on-page SEO, findings-native):** the page job's harvest evaluate ALSO extracts on-page SEO (`parse-seo-dom.ts`) → transient `HarvestedPageSeo` (one row per successful settle, `statusCode:200` since the runner throws before this on non-2xx; persisted in the same post-settle fence as `HarvestedLink`, never `page.url()`). The `broken-link-verify` job became the **single live-scan run builder**: it owns one `runId` + a shared `ensurePage` page map, reads BOTH transient tables, populates `CrawlPage` scalars, emits on-page findings (`mapOnPageSeoFindings` — duplicate/missing/thin over indexable non-login pages) merged with broken-link findings into ONE live-scan `CrawlRun` (still `score:null` — live SEO score deferred to Phase 3), then deletes both tables. The results page filters the one run into `BrokenLinksSection` (`broken_*`) + `OnPageSeoSection` (on-page types — disjoint sets, no cross-leak). `pruneHarvestedPageSeo()` (7-d) backstops stranded rows. Spec/plan: `docs/superpowers/archive/specs/2026-06-16-live-seo-onpage-extraction-design.md` + `archive/plans/2026-06-16-live-seo-onpage-extraction.md`. **C6 Phase 3 (live SEO score):** the builder also computes a coverage-aware live SEO score (`scoreLiveSeo`, forked from `computeHealthScore` minus crawl-depth + broken-links) from the on-page signals + SiteAudit counters and writes it to the live-scan `CrawlRun.score` (was `null`); `null` for noindex/login-walled (no indexable content) or <50% observed coverage. Coverage uses the actual `HarvestedPageSeo` row count (NOT `pagesComplete` — best-effort persist runs after the counter bump); schema coverage is computed before the transient rows are deleted. Surfaced on `OnPageSeoSection` (score + read-time coverage line); NO migration, `selectRuns` unchanged (the live score never displaces the sf-upload canonical score). Spec/plan: `docs/superpowers/archive/specs/2026-06-17-live-seo-score-design.md` + `archive/plans/2026-06-17-live-seo-score.md`. **C6 Phase 4 (external-link verification):** the builder also HEAD-only-verifies harvested `external-link` targets via `resolveExternalHead` (`lib/ada-audit/url-resolver.ts`) — never a GET, unlike the internal HEAD→GET pass — under its own cap `BROKEN_LINK_EXTERNAL_MAX_CHECKS` (default 300, 0 disables the pass entirely) and a soft time budget `BROKEN_LINK_EXTERNAL_TIME_BUDGET_MS` further clamped by the job's remaining time; broken = 404/410/5xx, everything else (401/403/405/429, other 4xx, throws) = `unconfirmed` (anti-bot-tolerant). Emits a separate `broken_external_links` run/page finding at `severity: 'warning'` (internal `broken_internal_links`/`broken_images` stay `critical`) — no zero-count finding; a cap/budget/harvest-truncation on the external pass alone flips the live-scan run to `status: 'partial'`. `BrokenLinksSection` renders it as a distinct amber warning tier alongside the internal (red) findings. **C6 hybrid discovery Increment 1 (sitemap miss-rate measurement, measurement-first):** the builder also computes `computeDiscoveryCoverage` (`lib/ada-audit/seo/discovery-coverage.ts`) — diffing the already-harvested same-domain internal-link targets against the `SiteAudit.discoveredUrls` baseline to measure how many reachable URLs the sitemap omitted — and stores it on the new nullable `CrawlRun.discoveryCoverageJson` (NOT a Finding — a Finding would inflate `priority.service`). Discovery provenance rides new nullable `SiteAudit.discoveryMode`/`discoveryCapped` (set by `discoverPages` now returning `{urls,mode,capped}` in the same first-writer-wins persist; `enqueueAudit` sets `pre-discovered`); the headline miss-rate is shown only for `mode:'sitemap'` non-capped audits (shallow-crawl/pre-discovered/capped → "not measured"; seed-url clients are `pre-discovered` by design — NOT bypassed). Read-time sibling `DiscoveryCoverageSection`. ZERO new fetches, no change to the audited page set — it only reads data already harvested. This is the gate number for hybrid-discovery Increment 2 (the actual crawler). Migration `20260704120000_discovery_coverage`. Spec/plan: `docs/superpowers/archive/specs/2026-07-04-hybrid-discovery-sitemap-miss-rate-design.md` + `archive/plans/2026-07-04-hybrid-discovery-sitemap-miss-rate.md`. **C6 Phase 5 (content similarity — LAST capability phase, measurement-first):** the page job also captures bounded normalized main-content text in-page (`parse-seo-dom.ts`, nav/header/footer/aside stripped, ≤30k, `wordCount` unchanged, SWC-safe) → transient `HarvestedPageSeo.contentText`/`contentTruncated` (deleted post-write, NEVER durable, never logged/selected outside the builder). The builder computes `computeContentSimilarity` (`lib/ada-audit/seo/content-similarity.ts`) — sha256 exact-dup + MinHash→exact-Jaccard near-dup over DF-boilerplate-filtered shingles — time-budget-guarded (skip→null if <`CONTENT_SIM_RESERVE_MS`=30s remains; try/catch fail-to-null, never fails the run write) and stores it on new nullable `CrawlRun.contentSimilarityJson` (migration `20260706130000_content_similarity`, additive). NOT a Finding, NO `scoreLiveSeo` change (promotion gated on SF-parity evidence + Kevin sign-off). Read-time `ContentSimilaritySection` (results page; share view unchanged). Spec/plan: `docs/superpowers/archive/specs/2026-07-06-content-similarity-design.md` + `archive/plans/2026-07-06-content-similarity.md`.
- **SEO Performance Reports (C10):** service-account auth (`lib/analytics/google/auth.ts`, key path from `GOOGLE_SA_KEY_FILE` — no OAuth routes, no DB credential row); provider layer (`lib/analytics/`) exposes `GA4Provider` (GA4 Data API via `google.analyticsdata('v1beta')`), `GSCProvider` (Search Console `searchanalytics.query`), and `ProspectsProvider` (CRM adapter stub → `ProspectsEntry` manual row → unmapped); pure HTML + inline-SVG builders in `lib/report/seo/` → durable `seo-report-render` job (group `seo-report:<id>`, concurrency 1, all GA4/GSC fetches complete and `metricsJson` persisted BEFORE `acquirePage()`); `SeoReportBatch`→`SeoReport` orchestration with `@@unique([scheduleId,scheduledFor])` + `@@unique([batchId,clientId])` idempotency; `metricsJson` snapshot retention swept by `pruneSeoReports()` (scheduled 730 d / ad-hoc 90 d, configurable via env); monthly non-system Schedule `seo-report-monthly` (operator-configured, not in `SYSTEM_SCHEDULES`). No `GoogleConnection` model, no `/api/google/connect|callback` routes.
- **Scan-completion email notifications (D7):** opt-in per site-audit request (checkbox "Email me when this finishes" on `SiteAuditForm` (C16: `SeoScanForm` retired), ALWAYS unchecked on load, hidden when no session email — `notifyAvailable` derived server-side from `getAuthSession().email` in the `/ada-audit` `(app)` page). `POST /api/site-audit {notify:true}` resolves the recipient SERVER-SIDE from the verified session (`getAuthSession`, client-supplied address ignored) and stamps `SiteAudit.notifyEmail`, threaded through `queueSiteAuditRequest`→`enqueueAudit`. Send seams: end of `runBrokenLinkVerify` (awaited-in-try/catch — never fails the builder) + its `onExhausted` → `complete` kind (runs for EVERY completed site audit); `failSiteAudit` (after the flip + `cancelJobsByGroup`) → `failed` kind to `NOTIFY_ADMIN_EMAIL`. Both gated on `notifyEmail != null` and the sent-marker. Schedules + bulk-queue omit `notifyEmail` → silent. Dark-by-default (`MAILGUN_API_KEY`/`MAILGUN_DOMAIN` unset → checkbox hidden + hooks no-op; NOT in `instrumentation.ts` fail-fast). From = `NOTIFY_FROM` (default `kevin@enrollmentresources.com`) with `NOTIFY_REPLY_TO` — the sending domain (`mg.enrollment.email`) does NOT DMARC-align with that From org domain (`enrollmentresources.com` is `p=none`), so `NOTIFY_FROM` is env-flippable to an aligned `@enrollment.email` sender post-smoke. Spec/plan: `docs/superpowers/archive/specs/2026-07-08-scan-email-notifications-design.md` + `archive/plans/2026-07-08-scan-email-notifications.md`.
- **Prospect sales audit view (C14):** sales scans a PROSPECT (non-client) domain and shares a stable token-gated PUBLIC report for meetings. `/sales` intake (cookie-gated) creates a `Prospect` (best-effort per-normalized-domain dedupe) + a FULL site audit (`prospectId` set, `clientId` null, NOT seoOnly — Accessibility + CWV need axe+PSI) through the normal queue. The public `/sales/[token]` page (`(public)` route group, `force-dynamic`, zero cookie-gated fetches) resolves token→prospect→**latest REPORTABLE** audit (complete + has the `seo-parser` live-scan run — the finalizer flips complete BEFORE the verifier writes that run, so a plain latest-complete rule would render empty SEO/GEO during the verifier window; a pending token shows a friendly auto-refreshing "being prepared" page). **The safety boundary is server-side curation:** `loadSalesReportData` picks the evidence (top patterns ≤4, examples ≤5, ONE representative screenshot per pattern via the bounded `representative-examples` loader — never fans out); the token grants access to NOTHING else. The screenshot route (`/api/sales/[token]/screenshot/[adaAuditId]/[filename]`, PUBLIC) authorizes ownership chain (token→prospect→child audit's parent `SiteAudit.prospectId`) AND curated-set membership (`curatedScreenshotSet` uses the SAME `topPatternIssues` selection → allowlist ⊆ rendered set; a guessed filename under an OWNED audit still 404s), pinning the child-audit id from the URL so an open report keeps loading its own images after a re-scan. Middleware public matchers are **anchored single-segment regex** (`^/sales/[^/]+$`, `^/api/sales/[^/]+/screenshot/[^/]+/[^/]+$`) — NEVER a `/sales`/`/api/sales/` prefix (that would expose the gated intake page + prospect APIs). Tokens `crypto.randomUUID`, 30-d TTL, extend-or-rotate on POST (GET read-only), swept by `cleanExpiredProspectSalesTokens` in `runCleanup`. Prospect audits are manual-class (never pruned by `pruneScheduledSiteAudits`), surface in unified recents with an additive "Prospect" badge (NOT a new `RecentType`), and `prospectId` is `onDelete: SetNull` (prospect DELETE orphans its audits, doesn't delete them). Honest labeling: performance is Lighthouse LAB data (no INP, TBT a proxy) — copy never claims "WCAG compliant" or "Core Web Vitals pass". `SALES_CONTACT_EMAIL` env (optional, default `kevin@enrollmentresources.com`). E-E-A-T + real GEO checks (llms.txt, answerability) are `// FUTURE` (partly gated by the no-AI-API decision). Spec/plan: `docs/superpowers/archive/specs/2026-07-09-prospect-sales-audit-view-design.md` + `archive/plans/2026-07-09-c14-prospect-sales-audit-view.md`.
- **Browser pool:** `acquirePage()` blocks when both slots are in use; `releasePage()` closes the page and wakes the next waiter. Never hold a page across awaits you don't control.
- **SIGTERM handler** in `instrumentation.ts` calls `closeBrowser()` so Chrome doesn't orphan on deploy restarts
- **Recharts** lazy-loaded via `next/dynamic` to avoid SSR issues
- **File I/O** uses `fs/promises` (async) everywhere
- **JSON.parse** always wrapped in try-catch in API routes
- **Share URLs** use `NEXT_PUBLIC_APP_URL` env var (not request origin)
- **Dark mode:** Tailwind `darkMode: 'class'`; anti-FOUC `<script>` in layout sets `.dark` before React hydrates; `ThemeProvider` context exposes `theme`/`toggle`/`mounted`; every component uses `dark:` variants mapping `bg-white` → `dark:bg-navy-card`, `text-gray-*` → `dark:text-white/*`, `border-gray-*` → `dark:border-navy-border`, semantic status colors → `dark:bg-{color}-500/{opacity}`
- **Parallel agent pattern for large features:** schema changes first → agents own exclusive file sets in parallel → integration pass → `tsc --noEmit` → build → commit → deploy

## Tools in the app
| Route | Description |
|-------|-------------|
| `/ada-audit` | **Audits (C16 merged section):** site-wide + single-page ADA and SEO audits, SF CSV uploads (collapsed section under Scan Type = SEO), unified 5-source recents (Site ADA · Site SEO · Single Page · SF Upload badges, cursor-paginated, All/Mine + search/client-filter/session-delete). `/seo-audits` index 308s here (`permanentRedirect`); `/seo-audits/results/*`, share and `/seo-audits/diff` URLs retained — the registry `aliases: ['/seo-audits']` keeps `toolForPathname()` resolving them (`/seo-parser` → `/seo-audits` 308s still chain in front) |
| `/robots-validator` | robots.txt + sitemap.xml validation |
| `/quarter-grid` | Drag-and-drop quarterly planning for ~30 clients (v1/v2/v3) |
| `/rankmath-redirects` | WordPress redirect migration guide |
| `/clients` | Client management with domain matching |
| `/reports` | Automated branded per-client SEO performance PDFs (GA4 + Search Console + CRM Prospects), period-over-period, on-demand + monthly schedule |
| `/sales` | **Prospect Scans (C14, cookie-gated):** sales self-serve full site audits on PROSPECT (non-client) domains → a stable token-gated PUBLIC report at `/sales/[token]` for meetings/leave-behinds |
| `/settings` | Google service-account connection status + monthly-report schedule controls |

## ADA Audit specifics
- axe-core runs inside headless Chrome (puppeteer-core). Navigation uses `waitUntil: 'domcontentloaded'` (30 s budget) followed by a best-effort `waitForNetworkIdle({ idleTime: 500, timeout: 5_000 })` settle via `postLoadSettle` in `lib/ada-audit/page-load.ts`. The settle is non-fatal — analytics/chat poll traffic on real client sites would otherwise prevent network-idle from ever firing. CSS and fonts that block first-paint are already in the DOM at DCL; color-contrast checks still work.
- `wcagLevel`: `wcag21aa` (default, "Required") runs `['wcag2a','wcag2aa','wcag21a','wcag21aa']`; `wcag22aa` ("Aspirational") adds `['wcag22aa','best-practice']`
- Progress written to DB at each phase (5→10→20→75→82→95→100%); client polls every second and shows live bar + estimated time
- `runnerType` column on `AdaAudit`/`SiteAudit`: `'browser'` for new audits, `'jsdom'` for legacy
- `domElementCount` stored in result JSON — values < 50 trigger an "unreliable result" warning (JS-rendered SPA)
- Score formula: v1 (`scoring.ts`, frozen) = weighted penalty per impact ÷ log10(totalElements); **live scoring is v2-formula/v3-version** (`scoring-v2.ts`, see Key files) — density-based, computed at the findings-mapper seam (`ada-mapper.ts`), persisted on `CrawlRun.score`/`scoreBreakdown`
- `shareToken` on `AdaAudit` enables public read-only view at `/ada-audit/share/[token]`; C4 mirrors this on `SiteAudit` → `/ada-audit/site/share/[token]` (both prefixes in `middleware.ts` `isPublicPath`; 30-d TTL, expired tokens nulled by cleanup). The site share page renders `SiteAuditResultsView` in `shareMode` — zero cookie-gated fetches (no triage, no by-violation view, no row expansion, no internal links)
- **C4 exports** on the site results page (`SiteAuditExportBar`, complete audits only): violations CSV + changes CSV (`/api/site-audit/[id]/csv[?sheet=changes]`), VPAT scaffold (`…/vpat`), branded PDF report (POST `…/report` enqueues `report-render` → poll `…/report/status` — `ready` requires stamp AND file on disk → GET streams). All 409 `no_findings_run` on pre-A2 audits. Report PDFs live in `REPORTS_DIR` (`${DATA_HOME}/reports` in prod), one file per audit, deleted on audit DELETE (jobs cancelled first) and by scheduled retention's snapshot sweep
- Site audits discover pages via robots.txt `Sitemap:` directives → `/sitemap.xml` → `/sitemap_index.xml` → `/wp-sitemap.xml` → `.xml.gz` → shallow crawl fallback; hard cap 1000 pages; per-site concurrency = 2 (configurable via `SITE_AUDIT_CONCURRENCY`, the `site-audit-page` job type's slots), browser pool size = 4 (configurable via `BROWSER_POOL_SIZE`); Chrome recycling lives in `browser-pool.ts`: a pool-global draining gate every `SITE_AUDIT_BROWSER_RECYCLE_PAGES` pages served (prod 15) plus a 60s idle close
- **Queue:** site audits enter `queued` status (FIFO), only one runs at a time; `discoveredUrls` stored as JSON on the SiteAudit row so queued audits don't re-crawl
- **Results views:** C18 shell = shared header + Accessibility | SEO tabs (`SiteAuditResultsShell`, `?resultTab=` sync); Accessibility tab: sort/filter toolbar (impact pills, sort dropdown), table vs sitemap tree toggle, triage toggle in the Pages-with-Issues section header, Pages with Issues paginated at 25, expandable site-wide-pattern cards (bounded `pattern-sample` loader), clean pages in separate collapsible section; SEO tab: the C6 live-scan section stack
- Chrome executable path: `/usr/bin/google-chrome` (override with `CHROME_EXECUTABLE` env var)
- **Lighthouse provider:** controlled by `LIGHTHOUSE_PROVIDER` (`pagespeed` | `local` | `off`). Default is `local` in code, `pagespeed` in the deployed `ecosystem.config.js`. PSI uses Google's infrastructure; expect score variance versus historical local-LH numbers. Per-page PSI failures fail the Lighthouse portion only — axe + PDFs still run.

## Do not
- Use interactive `prisma.$transaction(async tx => ...)` anywhere — array-form `$transaction([...])` only. Interactive transactions hold SQLite's write lock across event-loop round-trips; concurrent pdfjs parsing starves the loop and every other writer times out ("Operations timed out", 2026-06-10 production incident). Express conditional logic in SQL (`EXISTS` predicates) instead, and set `updatedAt` manually in raw statements (`Date.now()` — storage is integer ms; raw SQL bypasses `@updatedAt`).
- Add AI/LLM API features (Anthropic or any other provider) — decided 2026-07-08: no plans to use any AI API; all AI stays the skill-handoff clipboard flow (pat_/srt_/krt_/qct_). Reopening this is a Kevin decision recorded in the roadmap tracker's Gated decisions, never a session call
- Use `npm ci` on production (RunCloud uses `npm install`)
- Trust request origin headers for share URLs — use `NEXT_PUBLIC_APP_URL`
- Increase `BROWSER_POOL_SIZE` above 4 without first checking VPS memory headroom — each Chrome page is ~150-200 MB resident
